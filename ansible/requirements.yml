---
# Benötigte Ansible-Collections. Installieren mit:
#   ansible-galaxy collection install -r requirements.yml

collections:
  # community.docker: docker_compose_v2 (Container bauen/starten)
  - name: community.docker
    version: ">=3.4.0"
  # ansible.posix: synchronize (rsync-basierte Dateiübertragung)
  - name: ansible.posix
    version: ">=1.5.0"
